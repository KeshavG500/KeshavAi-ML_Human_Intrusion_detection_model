# ============================================================
# Human Intrusion Detection System — Requirements
# Python 3.10+  |  CUDA 11.8+
# ============================================================

# ── Core Deep Learning ──────────────────────────────────────
torch==2.2.2
torchvision==0.17.2
torchaudio==2.2.2
ultralytics==8.2.0            # YOLOv8 / YOLOv9

# ── ONNX & Inference Optimization ───────────────────────────
onnx==1.16.0
onnxruntime-gpu==1.18.0
tensorrt                      # optional: install via NVIDIA separately

# ── Tracking ────────────────────────────────────────────────
lap==0.4.0                    # ByteTrack dependency
scipy==1.13.0
filterpy==1.4.5               # DeepSORT Kalman filter

# ── Computer Vision ─────────────────────────────────────────
opencv-python==4.9.0.80
opencv-python-headless==4.9.0.80
Pillow==10.3.0
albumentations==1.4.6         # augmentation pipeline
imageio==2.34.1
imageio-ffmpeg==0.5.1

# ── Zone Logic ──────────────────────────────────────────────
Shapely==2.0.4                # polygon / line-crossing math
numpy==1.26.4

# ── API & Server ────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.29.0
websockets==12.0
python-multipart==0.0.9
pydantic==2.7.1
pydantic-settings==2.2.1
httpx==0.27.0                 # async HTTP client for tests

# ── Auth & Security ─────────────────────────────────────────
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
slowapi==0.1.9                # rate limiting

# ── Database & Caching ──────────────────────────────────────
redis==5.0.4
aioredis==2.0.1
sqlalchemy[asyncio]==2.0.30
asyncpg==0.29.0               # async PostgreSQL driver
alembic==1.13.1               # migrations

# ── Experiment Tracking ─────────────────────────────────────
mlflow==2.13.0
wandb==0.17.0

# ── Monitoring ──────────────────────────────────────────────
prometheus-client==0.20.0
prometheus-fastapi-instrumentator==7.0.0

# ── Data Download & Utils ───────────────────────────────────
fiftyone==0.24.1              # dataset management
pycocotools==2.0.7
tqdm==4.66.4
requests==2.32.3
python-dotenv==1.0.1
PyYAML==6.0.1
loguru==0.7.2

# ── Testing ─────────────────────────────────────────────────
pytest==8.2.0
pytest-asyncio==0.23.7
pytest-cov==5.0.0


